# Doc drift changelog

What the CopilotKit docs changed under this repo, written by the sync on
`/doc-sync`. Only pages that actually moved are recorded — a sync that finds
everything unchanged writes nothing here at all.

Holds the 3 most recent dated entries. When a change lands on a fourth
date, the oldest entry is dropped. Entries are counted, not aged, so a gap of
weeks between changes does not expire anything.

## 2026-09-04

### 08:08 UTC — 6 pages, highest severity high

**High — Headless Threads**

`/ag2/headless-threads` · route `/headless-threads` · under “Configure your Runtime with CopilotKit Intelligence”

2 code lines, 22 prose lines changed.

````diff
- Your `CopilotRuntime` must be connected to CopilotKit Intelligence before the thread UI can list and resume conversations. That connection is the `intelligence` option below — a `CopilotKitIntelligence` instance. If your app came from a CLI starter, this Runtime configuration is generated for you. Otherwise, follow [Connect your runtime to Intelligence](/ag2/premium/connect-your-runtime) for the full constructor, then return here to add the headless UI. Thread names are automatically generated by the LLM after the first message — you can disable this with `generateThreadNames: false`.
+ Your `CopilotRuntime` must be connected to CopilotKit Intelligence before the thread UI can list and resume conversations. That connection is the `intelligence` option below — a `CopilotKitIntelligence` instance. If your app came from a CLI starter, this Runtime configuration is generated for you. Otherwise, follow [Connect your runtime to Intelligence](/ag2/intelligence/connect-your-runtime) for the full constructor, then return here to add the headless UI. Thread names are automatically generated by the LLM after the first message — you can disable this with `generateThreadNames: false`.
- apiKey: process.env.INTELLIGENCE_API_KEY!,
+ apiKey: process.env.CPK_INTELLIGENCE_API_KEY!,
- CLI-created starters write the cloud-hosted platform URLs and project-scoped `INTELLIGENCE_API_KEY` to `.env`; keep that key server-side. Existing Intelligence-enabled apps should keep their current server-side Runtime configuration. Production self-hosting uses the same React APIs and is deployed with CopilotKit Engineering through [Self-host CopilotKit Intelligence](/ag2/premium/self-hosting).
+ CLI `init` and its `create` alias write the cloud-hosted platform URLs,
+ `SL_ENABLED`, project-scoped `CPK_INTELLIGENCE_API_KEY`, and optional
+ `CPK_TELEMETRY_ID` to `.env`.
````

**Low — Inspector**

`/ag2/inspector` · route `/inspector` · under “Showing or hiding the Inspector”

2 prose lines changed.

````diff
- a **different credential** from the server-side `INTELLIGENCE_API_KEY` that
+ a **different credential** from the server-side `CPK_INTELLIGENCE_API_KEY` that
````

**High — Reading agent state**

`/ag2/shared-state/read` · route `/shared-state/read` · under “Use the `useAgent` Hook”

26 code lines, 2 headings, 7 prose lines changed.

````diff
- optionally provide an initial state.
+ initialize missing UI-owned state after the connected agent is ready.
+ import { useEffect } from "react";
- const { agent } = useAgent({
+ const { agent, isReady } = useAgent({
- initialState: { language: "english" }  // optionally provide an initial state
+ const state = (agent.state ?? {}) as Partial<AgentState>;
+ useEffect(() => {
````

**High — Writing agent state**

`/ag2/shared-state/write` · route `/shared-state/write` · under “Call `setState` function from the `useAgent` hook” · in a `tsx` block

12 code lines changed.

````diff
+ import { useEffect } from "react";
- const { agent } = useAgent({ // [!code highlight]
+ const { agent, isReady } = useAgent({
- initialState: { language: "english" }  // optionally provide an initial state
+ const state = (agent.state ?? {}) as Partial<AgentState>;
+ useEffect(() => {
+ if (!isReady || state.language !== undefined) return;
+ agent.setState({ ...(agent.state ?? {}), language: "english" });
````

**High — Thread & History Lifecycle**

`/ag2/threads-lifecycle` · route `/threads-lifecycle` · under “The lifecycle at a glance”

2 code lines, 8 prose lines changed.

````diff
- 2. **Run.** Messages and tool calls stream under that `threadId`. If a server-side store is configured (CopilotKit Intelligence, or a persisting `AgentRunner`), they are persisted as they happen so the thread can be replayed later. A runtime with no persistence layer keeps nothing server-side. See [Threads & Persistence Architecture](/ag2/premium/threads-explained) for the full server-side model.
+ 2. **Run.** Messages and tool calls stream under that `threadId`. If a server-side store is configured (CopilotKit Intelligence, or a persisting `AgentRunner`), they are persisted as they happen so the thread can be replayed later. A runtime with no persistence layer keeps nothing server-side. See [Threads & Persistence Architecture](/ag2/intelligence/threads-explained) for the full server-side model.
- Replay requires a **server-side store to replay from**: CopilotKit Intelligence, or a persisting `AgentRunner` (e.g. the SQLite runner). A self-hosted runtime with no persistence layer has nothing to replay, so `connectAgent()` returns an empty stream and the conversation starts blank. If history isn't restoring, check that a store is configured, not the client code. The [Persistence Architecture](/ag2/premium/threads-explained) page covers how replay works server-side.
+ Replay requires a **server-side store to replay from**: CopilotKit Intelligence, or a persisting `AgentRunner` (e.g. the SQLite runner). A self-hosted runtime with no persistence layer has nothing to replay, so `connectAgent()` returns an empty stream and the conversation starts blank. If history isn't restoring, check that a store is configured, not the client code. The [Persistence Architecture](/ag2/intelligence/threads-explained) page covers how replay works server-side.
- apiKey: process.env.INTELLIGENCE_API_KEY!,
+ apiKey: process.env.CPK_INTELLIGENCE_API_KEY!,
- [Connect your runtime to Intelligence](/ag2/premium/connect-your-runtime) covers the
+ [Connect your runtime to Intelligence](/ag2/intelligence/connect-your-runtime) covers the
````

**Low — Overview**

`/ag2/threads` · route `/threads` · under “Next steps”

6 prose lines changed.

````diff
- - **Understand the architecture:** [Threads & Persistence Architecture](/ag2/premium/threads-explained) — event replay, live reconnection, synchronization, locking, and lifecycle behavior
- - **Use the hosted platform:** [Cloud-hosted CopilotKit Intelligence](/ag2/premium/managed-intelligence-platform) — create and manage the project where your app stores threads and runtime credentials
- - **Plan production self-hosting:** [Self-host CopilotKit Intelligence](/ag2/premium/self-hosting) — work with CopilotKit Engineering to run the Threads platform in your Kubernetes environment
+ - **Understand the architecture:** [Threads & Persistence Architecture](/ag2/intelligence/threads-explained) — event replay, live reconnection, synchronization, locking, and lifecycle behavior
+ - **Use the hosted platform:** [Cloud-hosted CopilotKit Intelligence](/ag2/intelligence/managed-intelligence-platform) — create and manage the project where your app stores threads and runtime credentials
+ - **Plan production self-hosting:** [Self-host CopilotKit Intelligence](/ag2/intelligence/self-hosting) — work with CopilotKit Engineering to run the Threads platform in your Kubernetes environment
````

---

## 2026-08-31

### 19:25 UTC — 22 pages, highest severity none

**Info — Introduction**

`/ag2` · routes `/`, `/doc-sync`

Now tracked for the first time.

**Info — AG-UI**

`/ag2/ag-ui` · route `/ag-ui`

Now tracked for the first time.

**Info — Authentication**

`/ag2/auth` · route `/auth`

Now tracked for the first time.

**Info — Copilot Runtime**

`/ag2/copilot-runtime` · route `/copilot-runtime`

Now tracked for the first time.

**Info — Headless UI**

`/ag2/custom-look-and-feel/headless-ui` · route `/custom-look-and-feel/headless-ui`

Now tracked for the first time.

**Info — Slots**

`/ag2/custom-look-and-feel/slots` · route `/custom-look-and-feel/slots`

Now tracked for the first time.

**Info — Frontend Tools**

`/ag2/frontend-tools` · route `/frontend-tools`

Now tracked for the first time.

**Info — State Rendering**

`/ag2/generative-ui/state-rendering` · route `/generative-ui/state-rendering`

Now tracked for the first time.

**Info — Tool Rendering**

`/ag2/generative-ui/tool-rendering` · route `/generative-ui/tool-rendering`

Now tracked for the first time.

**Info — Your Components · Display-only**

`/ag2/generative-ui/your-components/display-only` · route `/generative-ui/your-components/display-only`

Now tracked for the first time.

**Info — Your Components · Interactive**

`/ag2/generative-ui/your-components/interactive` · route `/generative-ui/your-components/interactive`

Now tracked for the first time.

**Info — Headless Threads**

`/ag2/headless-threads` · route `/headless-threads`

Now tracked for the first time.

**Info — Inspector**

`/ag2/inspector` · route `/inspector`

Now tracked for the first time.

**Info — Prebuilt Components**

`/ag2/prebuilt-components` · route `/prebuilt-components`

Now tracked for the first time.

**Info — Threads Drawer**

`/ag2/prebuilt-components/copilot-threads-drawer` · route `/prebuilt-components/copilot-threads-drawer`

Now tracked for the first time.

**Info — Programmatic Control**

`/ag2/programmatic-control` · route `/programmatic-control`

Now tracked for the first time.

**Info — Quickstart**

`/ag2/quickstart` · route `/quickstart`

Now tracked for the first time.

**Info — Readables**

`/ag2/readables` · route `/readables`

Now tracked for the first time.

**Info — Reading agent state**

`/ag2/shared-state/read` · route `/shared-state/read`

Now tracked for the first time.

**Info — Writing agent state**

`/ag2/shared-state/write` · route `/shared-state/write`

Now tracked for the first time.

**Info — Overview**

`/ag2/threads` · route `/threads`

Now tracked for the first time.

**Info — Thread & History Lifecycle**

`/ag2/threads-lifecycle` · route `/threads-lifecycle`

Now tracked for the first time.
